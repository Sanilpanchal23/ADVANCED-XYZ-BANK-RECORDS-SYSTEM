public interface CheckerPrinter {
    // Define a public interface named CheckerPrinter.

    // Method to check the eligibility of a customer.
    boolean checkEligibility(Customer customer);
    // Declare a method called checkEligibility that takes a Customer object as a parameter and returns a boolean.

    // Method to print details of a customer.
    void printCustomerDetails(Customer customer);
    // Declare a method called printCustomerDetails that takes a Customer object as a parameter and returns nothing.
}
